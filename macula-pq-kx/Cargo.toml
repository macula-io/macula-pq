[package]
name = "macula-pq-kx"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "SecP384r1MLKEM1024 and SecP256r1MLKEM768 hybrid key exchange groups for rustls, on macula-mlkem's ML-KEM"
keywords = ["post-quantum", "ml-kem", "rustls", "key-exchange", "hybrid"]
categories.workspace = true

# Versions and the reasons for them are in the workspace Cargo.toml.
[dependencies]
macula-mlkem = { workspace = true }
rustls = { workspace = true }

[dev-dependencies]
aws-lc-rs = { workspace = true }
